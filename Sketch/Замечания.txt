
void InitUART(char Count,int* BaudRates)  //Это тоже не очень хорошо record надо в исходном коде сделать.  А при трансляции уже в индивидуальные процедуры. 
{
  if (Count>1) return; //
  for (i=0,i<Count,i++)  - так плохо, лучше индивидуальная процедура. 

Зделать 
FastUART
(while(1) + посылка одного байта). 
  